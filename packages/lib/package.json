{
  "name": "@messaging-gateway/lib",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf output && rm -f tsconfig.tsbuildinfo",
    "lint": "echo \"Warn: no lint specified\"",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "main": "./output/Index.js",
  "types": "./output/Index.d.ts",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@line/bot-sdk": "^9.2.2",
    "node-jose": "^2.2.0"
  },
  "devDependencies": {
    "@types/node-jose": "^1.1.13"
  }
}
